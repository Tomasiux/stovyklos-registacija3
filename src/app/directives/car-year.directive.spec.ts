import { kidAgesDirective } from './car-year.directive';

describe('kidAgesDirective', () => {
  it('should create an instance', () => {
    const directive = new kidAgesDirective();
    expect(directive).toBeTruthy();
  });
});